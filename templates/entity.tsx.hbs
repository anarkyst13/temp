import React from 'react';
import { Styled{{pascalCase name}} } from './{{pascalCase name}}.styles';
import { {{pascalCase name}}Props } from './types';


const {{pascalCase name}}: React.FC<{{pascalCase name}}Props> = ({ {{argsList}} }) => {
  // PLACE FOR CUSTOM HOOKS
  // Example: const [state, setState] = useState(initialValue);
  // Example: const data = useCustomHook();

  // PLACE FOR LOGIC
  // Example: const handleClick = () => { ... };
  // Example: useEffect(() => { ... }, []);

  return (
    <Styled{{pascalCase name}}>
      {{textProp}}
    </Styled{{pascalCase name}}>
  );
};

export default {{pascalCase name}};